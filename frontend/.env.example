# 로컬 개발: cp .env.example .env.development (이미 커밋되어 있어 보통 불필요)
# 프로덕션 빌드: frontend/.env.production은 gitignore 대상이며,
#   실제 값은 GitLab CI/CD 변수(VITE_API_BASE_URL)로 주입합니다.
VITE_API_BASE_URL=http://localhost:3000
VITE_DEFAULT_COUPON_ID=1
