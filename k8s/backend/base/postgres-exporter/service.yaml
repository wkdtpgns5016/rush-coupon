apiVersion: v1
kind: Service
metadata:
  name: postgres-exporter
  # ServiceMonitor 가 이 라벨로 Service 를 선택한다.
  labels:
    app: postgres-exporter
spec:
  selector:
    app: postgres-exporter
  ports:
    - name: metrics
      port: 9187
      targetPort: metrics
