apiVersion: v1
kind: Service
metadata:
  name: backend
  # ServiceMonitor가 이 라벨로 Service를 선택한다.
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    # ServiceMonitor가 포트를 이름으로 참조한다. (Ingress는 번호 80으로 참조하므로 영향 없음)
    - name: http
      port: 80
      targetPort: 3000
